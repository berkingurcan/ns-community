# Enhanced Authentication Configuration Template
# Copy this file to .env.local and fill in your values

# =============================================================================
# REQUIRED: Supabase Configuration
# =============================================================================
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =============================================================================
# REQUIRED: NFT Collection for Access Gating
# =============================================================================
# The Solana public key of the NFT collection required for access
# Users must own at least one NFT from this collection
NEXT_PUBLIC_NFT_COLLECTION_ADDRESS=your_nft_collection_public_key_here

# =============================================================================
# REQUIRED: RPC Endpoint (Must Support DAS API)
# =============================================================================
# Choose ONE of the following options:

# Option 1: Helius (Recommended)
# Sign up at https://helius.xyz/ and get your API key
NEXT_PUBLIC_RPC_URL=https://rpc.helius.xyz/?api-key=your-helius-api-key

# Option 2: QuickNode (Alternative)
# Sign up at https://quicknode.com/ and get your endpoint
# NEXT_PUBLIC_RPC_URL=https://your-endpoint.solana-mainnet.quiknode.pro/your-api-key/

# Option 3: Other DAS-compatible provider
# NEXT_PUBLIC_RPC_URL=https://your-das-compatible-rpc-endpoint.com

# ⚠️  DO NOT USE STANDARD SOLANA RPC ENDPOINTS ⚠️
# These will not work with the NFT verification system:
# ❌ https://api.mainnet-beta.solana.com
# ❌ https://api.devnet.solana.com  
# ❌ https://api.testnet.solana.com

# =============================================================================
# OPTIONAL: Network Configuration
# =============================================================================
# Specify which Solana network your app should use
# This must match your RPC endpoint and NFT collection network
# Default: mainnet
NEXT_PUBLIC_SOLANA_NETWORK=mainnet

# Available options:
# - mainnet    (for production apps)
# - devnet     (for development/testing)
# - testnet    (for testing)

# =============================================================================
# DEVELOPMENT ONLY: Additional Configuration
# =============================================================================
# These are only needed for development/testing scenarios

# Test Collection Address (for devnet testing)
# NEXT_PUBLIC_NFT_COLLECTION_ADDRESS_DEVNET=your_devnet_collection_address

# Development RPC (for devnet)
# NEXT_PUBLIC_RPC_URL_DEVNET=https://rpc.helius.xyz/?api-key=your-key&network=devnet

# =============================================================================
# CONFIGURATION VALIDATION CHECKLIST
# =============================================================================
# Before running your app, ensure:
# 
# ✅ All REQUIRED variables above are set
# ✅ NFT collection address is a valid Solana public key
# ✅ RPC endpoint supports DAS API (not standard Solana RPC)
# ✅ Network setting matches your RPC endpoint
# ✅ Network setting matches your NFT collection network
# 
# The app will validate your configuration on startup and show
# specific error messages if anything is misconfigured.

# =============================================================================
# TESTING SCENARIOS
# =============================================================================
# 
# Test Network Mismatch:
# 1. Set NEXT_PUBLIC_SOLANA_NETWORK=devnet
# 2. Keep mainnet RPC endpoint
# 3. App should detect mismatch and show error
#
# Test DAS API Validation:
# 1. Set NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com
# 2. App should detect lack of DAS support and show warning
#
# Test Invalid Collection:
# 1. Set invalid NEXT_PUBLIC_NFT_COLLECTION_ADDRESS
# 2. App should detect invalid format and show error

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES
# =============================================================================
#
# For Vercel:
# - Add these variables in your Vercel project settings
# - Do NOT commit .env.local to your repository
#
# For other platforms:
# - Ensure all NEXT_PUBLIC_* variables are available at build time
# - RPC endpoints should be reliable and have good uptime
# - Consider using multiple RPC endpoints for redundancy
#
# Security Notes:
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Never put sensitive server-side secrets in NEXT_PUBLIC_* variables
# - RPC API keys are visible to users (this is normal for client-side apps)
# - Ensure your Supabase row-level security (RLS) policies are properly configured